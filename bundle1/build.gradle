dependencies {
    compile project(":api.api1")
}

jar {
    manifest {
        instruction 'Bundle-Activator', 'com.mycompany.osgi.demo.bundle1.Activator'
        instruction 'Private-Package', 'com.mycompany.osgi.demo.bundle1'
    }
}
